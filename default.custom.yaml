# default.custom.yaml

patch:

  schema_list:
    - schema: array30             # 行列30
    - schema: luna_pinyin_tw      # 朙月拼音*臺灣正體
    - schema: luna_pinyin_simp    # 朙月拼音*简化字

  key_binder:
    bindings:
      - {accept: "Control+p", send: Up, when: composing}
      - {accept: "Control+n", send: Down, when: composing}
      - {accept: "Control+b", send: Left, when: composing}
      - {accept: "Control+f", send: Right, when: composing}
      - {accept: "Control+a", send: Home, when: composing}                    # 跳至句首
      - {accept: "Control+e", send: End, when: composing}                     # 跳至句末
      - {accept: "Control+d", send: Delete, when: composing}
      - {accept: "Control+k", send: "Shift+Delete", when: composing}
      - {accept: "Control+h", send: BackSpace, when: composing}
      - {accept: "Control+g", send: Escape, when: composing}
      - {accept: "Control+bracketleft", send: Escape, when: composing}
      - {accept: "Control+y", send: Page_Up, when: composing}
      - {accept: "Alt+v", send: Page_Up, when: composing}
      - {accept: "Control+v", send: Page_Down, when: composing}
      - {accept: ISO_Left_Tab, send: "Shift+Left", when: composing}
      - {accept: "Shift+Tab", send: "Shift+Left", when: composing}
      - {accept: Tab, send: "Shift+Right", when: composing}
      - {accept: minus, send: Page_Up, when: has_menu}
      - {accept: equal, send: Page_Down, when: has_menu}
      - {accept: comma, send: Page_Up, when: paging}
      - {accept: period, send: Page_Down, when: has_menu}
      - {accept: "Control+Shift+1", select: .next, when: always}              # 拼音／行列
      - {accept: "Control+Shift+2", toggle: ascii_mode, when: always}         # 西文／中文
      - {accept: "Control+Shift+9", toggle: full_shape, when: always}         # 全角／半角
      - {accept: "Control+Shift+0", toggle: simplification, when: always}     # 簡／繁
      - {accept: "Control+Shift+8", toggle: extended_charset, when: always}   # 常用／增廣
      - {accept: "Control+Shift+exclam", select: .next, when: always}
      - {accept: "Control+Shift+at", toggle: ascii_mode, when: always}

  switcher:
    abbreviate_options: true
    caption: "〔方案選單〕"
    fold_options: true
    hotkeys:
      - "Control+grave"
      # - "F4"
    save_options:
      - full_shape
      - simplification
      - extended_charset
